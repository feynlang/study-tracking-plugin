{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|str_replace|create_file",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/log_progress.sh"
          }
        ]
      }
    ]
  }
}
