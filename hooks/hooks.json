{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|str_replace|create_file",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/log_progress.py"
          }
        ]
      }
    ]
  }
}
